# Production Backend URL (you'll need to deploy backend separately)
REACT_APP_BACKEND_URL=http://localhost:5000

# For production, you'll need to deploy the Flask backend to a service like:
# Heroku, Railway, Render, or PythonAnywhere
# Then update this URL to your deployed backend
